services:
  postgres:
    image: postgres:latest
    container_name: jessval-postgres
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=RiEiJu2509@@
      - POSTGRES_DB=paqueteria
    ports:
      - "5434:5432"